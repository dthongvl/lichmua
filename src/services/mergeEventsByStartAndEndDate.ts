import Event from '../types/event';

const mergeEventsByStartAndEndDate = (events: Array<Event>): Array<Event> => {
  console.log(events);
  return events;
};

export default mergeEventsByStartAndEndDate;
